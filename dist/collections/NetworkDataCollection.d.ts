import { NetworkData } from '../structures/NetworkData';
import { Collection } from '../framework/Collection';
export declare class NetworkDataCollection extends Collection<NetworkData> {
    for(symbol: string): NetworkData;
}
