import { Agent } from '../structures/Agent';
import { Collection } from '../framework/Collection';
export declare class AgentCollection extends Collection<Agent> {
    forGroup(groupid: any): AgentCollection;
}
